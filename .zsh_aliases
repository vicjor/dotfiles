# Aliases
alias Downloads="~/Downloads/"
alias ze="nvim ~/.zshrc"
alias za="source ~/.zshrc"
alias ve="vim ~/.vimrc"
alias dev="cd ~/dev"
alias vim="nvim"

# Gjensidige
alias lpd="npm i --legacy-peer-deps"

# Make sure this file is imported in .zshrc by adding the line below
# source .zsh_aliases
